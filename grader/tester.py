""""
Handles testing of solutions.
"""
from common import executor


class Tester:
    @staticmethod
    def test(binary, inpFile, solFile):
        """ Executes a compiled binary in a secured sandbox.
        The binary is fed as input the content of inpFile and expected to output solFile.
        """
        pass

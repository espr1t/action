# Secure problem tests
<FilesMatch ".+\.(in|out|sol|cpp|py|java)$">
	AuthName "Authorized Users Only"
	AuthType Basic
	AuthUserFile /home/espr1tn/domains/action/.htpasswd
#	AuthUserFile "D:/WebSites/action/web/.htpasswd"
	require valid-user
 </FilesMatch>
